# Copy to .env.local (gitignored) and fill in.
# Required by POST /api/route-plan to generate the route plan's narrative.
# Without it, the route still returns the deterministic ranked plans — only
# the written "why this route" prose is skipped.
ANTHROPIC_API_KEY=

