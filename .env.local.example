# Copy to .env.local (gitignored) and fill in.
# Required by POST /api/spot-match to generate the supplier-match narrative.
# Without it, the route still returns the deterministic ranking — only the
# written "reasons" prose is skipped.
ANTHROPIC_API_KEY=

